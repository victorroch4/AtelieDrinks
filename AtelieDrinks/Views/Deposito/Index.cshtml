@model IEnumerable<AtelieDrinks.Models.Deposito>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.setor_armazenamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.nome_item)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.medida_de_armazenamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.quantidade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.custo_tecnico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descricao_observacao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.setor_armazenamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nome_item)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.medida_de_armazenamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.quantidade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.custo_tecnico)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descricao_observacao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_item">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_item">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_item">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
