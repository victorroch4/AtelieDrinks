@model IEnumerable<AtelieDrinks.Models.Custo_deslocamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tipo_deslocamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.valor_tipo_deslocamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.custo_tipo_deslocamento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tipo_deslocamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.valor_tipo_deslocamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.custo_tipo_deslocamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_taxa_deslocamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_taxa_deslocamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_taxa_deslocamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
